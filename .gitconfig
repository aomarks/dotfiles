[user]
	name = Alexander Marks
	# Require explicit user.name and user.email settings. Don't guess them.
	useConfigOnly = true
[core]
	editor = /usr/bin/env vim
[push]
	default = current
[pull]
	ff = only
